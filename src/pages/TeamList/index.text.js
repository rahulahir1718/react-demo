import { render, screen } from "@testing-library/react";
import TeamList from ".";

describe("async teams list", ()=>{
    test("get teams", ()=>{
        //arrange
        render(<TeamList/>);

        //act

        //assert
        const test = screen.getbyC
    });
});